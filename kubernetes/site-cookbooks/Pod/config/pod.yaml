apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: MyApp
spec:
  containers:
  - name: nginx-container
    image: nginx
    ports:
    - containerPort: 80
status:
  phase: newPod
